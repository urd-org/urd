'use strict';

const assert = require('bsert');

const consensus = require('./consensus');

/**
 * @returns {Array} [valid, reason, score]
 */
exports.blockCheck = function blockCheck(block) {
};

/**
 * @returns {Array} [valid, reason, score]
 */
exports.blockVerify = function blockVerify(block) {
};
