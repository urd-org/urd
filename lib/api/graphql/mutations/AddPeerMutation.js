const { GraphQLBoolean } = require("graphql");

const AddPeerMutation = {
  type: GraphQLBoolean,
  args: {},
  resolve: async (request, args, context) => {

  }
};

module.exports = AddPeerMutation;
