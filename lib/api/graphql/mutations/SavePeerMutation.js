const { GraphQLBoolean } = require("graphql");

const SavePeerMutation = {
  type: GraphQLBoolean,
  args: {},
  resolve: async (request, args, context) => {

  },
};

module.exports = SavePeerMutation;
