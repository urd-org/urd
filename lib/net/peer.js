const NetAddress = require('./netaddress');

/*!
 * peer.js - Handling packets from specific peers at inbound and outbound level
 *
 */

class Peer extends EventEmitter {

  /**
   * Create a peer.
   * @constructor
   */
  constructor() {
    super();
  }
}
